# vibecoding / AI session docs — not part of the shipped codebase
CLAUDE.md
CONTEXT.md
**/CONTEXT.md
*_handoff.md
*_handoff_*.md
handoff*.md
HANDOFF*.md

# common OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Node
node_modules/
.next/
dist/

# env / secret material — never commit (see docs/SECRETS_ROTATION.md)
.env
.env.*
*.env
!.env.example
# local-only secret/override files dropped next to tooling scripts
scripts/*.local.*
*.local.env
# credential / key material (do NOT ignore the legitimate .py tooling scripts)
secrets.json
*credentials*.json
service-account*.json
*.pem
*.key
*.p12
# Jobber rotating refresh-token store (written by jobber_auth.py at runtime)
src/app/data/jobber_token.json
src/app/data/jobber_token.json.tmp
